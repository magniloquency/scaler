sphinx
shibuya
sphinx-substitution-extensions
sphinx-tabs
sphinx-copybutton
nbsphinx
# Patches only: a kernel minor moves the bundled Pyodide series, which is the ABI the wasm client
# wheel is built for (PYODIDE_XBUILDENV_VERSION in scripts/build_wasm.sh), and a jupyterlite-sphinx
# minor changes the JupyterLite site layout the docs' ``?path=`` links depend on.
jupyterlite-pyodide-kernel>=0.8.2,<0.9
jupyterlite-sphinx>=0.23.0,<0.24
